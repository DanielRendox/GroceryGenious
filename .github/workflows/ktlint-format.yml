name: ktlint-format

on:
  pull_request:
    branches: [ develop, production ]

  workflow_dispatch:

jobs:
  ktlint-format:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Restore cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: 17

      - name: Ensure gradlew is executable
        run: chmod +x ./gradlew

      - name: Run Ktlint Format
        run: ./gradlew ktlintFormat

      - name: Check for changes
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            echo "Changes detected after running ktlintFormat."
          else
            echo "No changes detected."
            exit 0
          fi

      - name: Configure Git
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Commit and Push Changes
        env:
          GITHUB_TOKEN: ${{ secrets.ACTIONS_PUSH }}
        run: |
          git add .
          git commit -m "Apply ktlint format"
          git push https://x-access-token:${{ secrets.ACTIONS_PUSH }}@github.com/${{ github.repository }} HEAD:$(git rev-parse --abbrev-ref HEAD)
